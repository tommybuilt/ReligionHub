import { RouteSeoHead } from '@/components/route-seo-head';

export default function Head() {
  return (
    <RouteSeoHead
      title="Compare Religions"
      description="Select 2-4 religions to compare side-by-side with citation-backed facts on beliefs, practices, history, and demographics."
      path="/compare"    />
  );
}
